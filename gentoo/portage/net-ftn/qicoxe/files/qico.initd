#!/sbin/runscript

start() {
	ebegin "Starting qico daemon"
	start-stop-daemon --start -u fido:fido --pidfile /var/run/ftn/qico.pid --exec /usr/sbin/qico -- -d
	eend $?
}

stop() {
	ebegin "Stopping qico daemon"
	start-stop-daemon --stop --pidfile /var/run/ftn/qico.pid
	eend $?
}
